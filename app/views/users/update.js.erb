<% if @user.errors.any? %>
  $('.user-form-errors')
    .html('<%= j render "shared/error_messages", object: @user %>');
  if($('.user-form').is(":visible")) {
    $('html, body').animate({
      scrollTop: $('.user-info').position().top
    }, 500);
  }
<% end %>
